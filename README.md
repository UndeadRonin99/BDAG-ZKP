# Simple JavaScript Calculator

This project provides basic arithmetic operations implemented in JavaScript. It includes addition, subtraction, multiplication, and division, along with tests using Node's built-in test runner.

## Running Tests

```
npm test
```

## Demo

Open `index.html` in a browser to try the calculator in a simple web interface.
